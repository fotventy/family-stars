# 🌟 Family Stars - Environment Variables

# 🗄️ Database
# For development (SQLite)
# DATABASE_URL="file:./dev.db"
# For production (PostgreSQL)
DATABASE_URL="postgresql://username:password@host:port/database"

# 🔐 Authentication (обязательно сменить в продакшене!)
# Сгенерировать: openssl rand -base64 32
NEXTAUTH_SECRET="change_this_to_random_32_chars_min"

# 🌐 URLs
NEXTAUTH_URL="http://localhost:3000"
# Production: NEXTAUTH_URL="https://your-app.vercel.app"

# 🔑 Admin guard (опционально)
# Если задан — опасные API (wipe-database, init-admin, debug-* и т.д.)
# доступны только с заголовком X-Admin-Key: <ADMIN_SECRET>.
# В продакшене без ADMIN_SECRET эти эндпоинты возвращают 404.
# ADMIN_SECRET="your_admin_secret"

# 🌐 SSO — Google (опционально)
# Создать OAuth 2.0 Client: https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: https://your-domain/api/auth/callback/google
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""

# 🍎 SSO — Apple (опционально)
# Настроить Sign in with Apple в Apple Developer
# APPLE_ID=""
# APPLE_SECRET=""

# 💳 Подписка «Без рекламы»
# Секрет для верификации покупки (нативное приложение передаёт его в заголовке после IAP).
# Сгенерировать: openssl rand -hex 32
# SUBSCRIPTION_VERIFICATION_SECRET="..."

# 📱 Реклама (мобильное приложение, детская/семейная)
# ID баннеров из AdMob (для детского контента создайте блоки с тегом «для детей»).
# NEXT_PUBLIC_ADMOB_BANNER_ID="ca-app-pub-xxx/yyy"
# NEXT_PUBLIC_ADMOB_INTERSTITIAL_ID="ca-app-pub-xxx/zzz"

# 📧 Приглашения по email (опционально)
# При добавлении члена семьи с указанием email приглашение уйдёт на почту.
# Resend: https://resend.com — получить API Key, верифицировать домен для EMAIL_FROM
# RESEND_API_KEY="re_..."
# EMAIL_FROM="Семейные Звёздочки <noreply@yourdomain.com>"

# 📝 Instructions:
# 1. Copy to .env.local and set NEXTAUTH_SECRET (and DATABASE_URL for production)
# 2. For SSO add GOOGLE_* or APPLE_* and ensure user email exists in app (e.g. after register-family)
# 3. For production set all in Vercel (or your host) Environment Variables
